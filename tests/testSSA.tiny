main 
    var a, b, c, d;
    void function printSum(a, b);
        var c;
    {
        let c <- a + b;
        call OutputNum(c);
    };
    void function printDiv(a, b);
        var c;
    {
        let c <- a / b;
        call OutputNum(c);
    };
{
    let c <- 1 + 2;
    let d <- 3;

}.
/*
// printSum
-2: CONST #2
-1: CONST #1
0: ADDOP (-1) (-2)
1: write (0)

// __main__
2: read
3: read
4 call printSum
*/
