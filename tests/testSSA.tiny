main 
    var a, b, c;
{
    let a <- a + 1;
    if a > b then
        let a <- a + 1;
    fi;
    while a < b do
        let a <- a + 1;
    od;
    call OutputNum(a);
}.

/*

main
    var a, b;
{
    let a <- 2 + 1;
    if a < 2 then
        let a <- 2;
    else
        let b <- 1;
    fi;
    call OutputNum(a);
    call OutputNum(b);

}.

main
    var a;
{
    let a <- 2;
    if a < 2 then
        let a <- 1 + 2;
    fi;
    call OutputNum(a);

}.


main
    var a, b, c;
{
    let a <- call InputNum();
    let b <- a;
    if a > 5 then
        let a <- 4;
    else
        while a < 5 do
            let a <- a + 2;
            call OutputNum(b);
        od;
    fi;
}.





main
    var a, b;
    function performArithmetic(op, a, b); var c; {
        if op == 1 then
            let c <- a + b;
        else
            let c <- a - b;
        fi;

        return c;
    };
    void function printSum(a, b); var c; {
        let c <- call performArithmetic(1, a, b);
        call OutputNum(c);
    };
{
    let a <- call InputNum();
    let b <- call InputNum();
    if a < b then
        let a <- 2;
    else
        let b <- 2;
    fi;
    call printSum(a, b);
}.

    let b <- 3;
    if a < b then
        let a <- 2;
    fi;
    call OutputNum(a);

main 
    var a, b, c;
{
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();
    if a > 0 then
        if b > 0 then
            let a <- b;
        fi;
        let c <- a + b;
    fi;
    call OutputNum(a);
    call OutputNum(b);
    call OutputNum(c);
}.
*/
