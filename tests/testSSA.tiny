main 
    var a, b, c, d, e, y;
{
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- a;
    let d <- b;
    if a > b then
        while a < b do
            let a <- a + 2;
        od;

    fi;
}.
/*
        while a > b do
            let a <- a + 2;
        od;
-2: CONST #2;
-1: CONST #7;
0: read;
1: read;
2: MULOP(0)(1); lArg: a, rArg: b, No Dom Inst with Opcode
3: MULOP(0)(-1); lArg: a, rArg: , 2: MULOP(0)(1)
4: ADDOP(2)(3); lArg: _, rArg: _, No Dom Inst with Opcode
5: CMP(0)(1); lArg: a, rArg: b, No Dom Inst with Opcode
6: BGE(5)(7); lArg: , rArg: , No Dom Inst with Opcode {4, 5, 8}

7: NOP;
8: PHI(-2)(0); lArg: , rArg: , No Dom Inst with Opcode {4}
9: write(4); lArg: 


*/