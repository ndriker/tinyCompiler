main 
  var a, b, c, d;
{
  let a <- 5;
  let b <- a;
  if a == b then
    let d <- a + 1;
  fi  
  if a > b then
    let d <- b * 2;
  fi  
  if a < b then
    let d <- b / 2;
  fi
  let c <- 10;
  while c >= 0 do
    let c <- c - 1;
  od
  if c <= 10 then
    OutputNum(1);
  fi
  if c != 0 then
    OutputNum(-1);
  fi
}.